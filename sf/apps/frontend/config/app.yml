# You can find more information about this file on the symfony website:
# http://www.symfony-project.org/reference/1_4/en/11-App
all:
  # Данные для подключения робокассы
  billing:
    robokassa:
      # URL "боевого" сервиса оплаты
      url:                  http://test.robokassa.ru/Index.aspx
      # URL "тестового" сервиса оплаты
      test_url:             http://test.robokassa.ru/Index.aspx
      # имя пользователя в системе робокасса
      login:                artemk1980
      # Пароль 1
      pass1:                password1
      # Пароль 2
      pass2:                password2
      # Флаг тестового режима (влияет на то, какой URL будет использован)
      test:                 1